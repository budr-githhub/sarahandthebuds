@AGENTS.md

# CLAUDE.md

Claude Code-specific notes. Shared rules are imported above and remain canonical; do not duplicate them here.

- The remote is `budr-githhub/sarahandthebuds`.
- Run commands at the repository root: `pnpm build`, not Plan & Adapt's `pnpm --dir site build`.
- Start with [docs/dashboard.md](docs/dashboard.md) and use the [handoff template](docs/ai-playbook.md).
- Review copy through [docs/copy-deck.md](docs/copy-deck.md), then synchronize edits with `src/` in the same PR.
- Do not assume access to Codex's conversation, temporary checkout, or browser session. Re-read the branch and PR being reviewed.
